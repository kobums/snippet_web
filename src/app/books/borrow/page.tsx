"use client"

import AppShell from "@/components/layout/AppShell";
import BorrowedBooksPage from "@/components/pages/BorrowedBooksPage";

export default function BooksBorrowPage() {
  return (
    <AppShell>
      <BorrowedBooksPage />
    </AppShell>
  );
}
